#include <stdio.h>

int main () {

    // inizializzazione
    int i = 0;
    
    do {
        printf("Iterazione %d\n", i);
        i++; //iterazione
    } while(i < 5); //condizione

    return 0;
}
