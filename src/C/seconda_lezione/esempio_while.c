#include <stdio.h>

int main () {

    // inizializzazione
    int i = 0;
    // condizione
    while( 0 ) {
        printf("Iterazione %d\n", i);

        // iterazione
        //i++;
    }
    printf("No ciclo");
    return 0;
}
