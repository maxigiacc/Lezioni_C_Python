#include <stdio.h>

int main () {

    // inizializzazione
    int i = 0;
    
    // condizione
    while( i < 5) {
        printf("Iterazione %d\n", i);

        // iterazione
        i++;
    }

    return 0;
}
